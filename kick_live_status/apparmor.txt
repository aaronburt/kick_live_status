#include <tunables/global>

profile kick_live_status flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  /usr/bin/tini ix,
  /sbin/tini ix,
  /bin/sh ix,
  /bin/busybox ix,
  /usr/local/bin/docker-entrypoint.sh rix,
  /usr/local/bin/node ix,
  /usr/local/lib/** r,
  /usr/lib/** r,
  /lib/** r,

  /app/** r,
  /data/** r,

  /etc/ssl/** r,
  /etc/ca-certificates/** r,
  /etc/resolv.conf r,
  /etc/hosts r,
  /etc/nsswitch.conf r,

  /proc/** r,
  /proc/self/fd/** rw,

  /dev/null rw,
  /tmp/** rw,

  network inet stream,
  network inet6 stream,
  network inet dgram,
  network inet6 dgram,

  signal receive,
}
